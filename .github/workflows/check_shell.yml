name: check
on: [push]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check Shell Script
        run: |
          for f in *.sh; do
             bash -n "$f" && echo "✓ $f" || echo "✗ $f"
          done