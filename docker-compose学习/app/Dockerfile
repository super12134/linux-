# 使用官方 Node.js 镜像
FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 先复制 package.json，利用 Docker 缓存
COPY package.json .

# 安装依赖
RUN npm install --production

# 复制源代码
COPY index.js .

# 声明端口
EXPOSE 3000

# 启动命令
CMD ["node", "index.js"]
